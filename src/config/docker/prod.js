module.exports = {
    register: {
        endpoints: {
            createWithRut: '/api/register',
            getById: '/api/register',
            getByRut: '/api/register'
        }
    },
    doctor: {
        endpoints: {
            getByRut: '/api/doctor'
        }
    }
}